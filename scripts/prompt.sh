#!/usr/bin/env bash

# Tell build process to exit if there are any errors.
set -oue pipefail

# echo -e "\n" >> /etc/bashrc
# echo -e "# Setup the prompt." >> /etc/bashrc
# echo -e "source /usr/share/prompt/prompt.sh" >> /etc/bashrc
